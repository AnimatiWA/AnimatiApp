<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnimatiAppAndroid"
        tools:targetApi="31">
        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false" />
        <!-- Cambiado: activity_inicio ahora es la pantalla de inicio -->
        <activity
            android:name=".MainMenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Se registra la actividad sobre nosotros -->
        <activity
            android:name=".SobreNosotrosActivity"
            android:exported="true" />
        <activity
            android:name=".activity_inicio"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true" /> <!-- Se registra la actividad del carrito -->
        <activity
            android:name=".CartActivity"
            android:exported="true" /> <!-- Se registra la actividad galería -->
        <activity
            android:name=".Gallery"
            android:exported="true"></activity> <!-- Se registra la actividad contacto -->
        <activity
            android:name=".ContactoActivity"
            android:exported="true"></activity> <!-- Se registra la actividad registro -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true"></activity> <!-- Login -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"></activity>
    </application>

</manifest>